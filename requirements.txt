# ============================================================
#   EEG-Based Emotion Recognition using Deep CNN
#   Requirements File
#   Python 3.8+
# ============================================================

# ─── Core Deep Learning ─────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# ─── Numerical Computing ────────────────────────────────────
numpy>=1.23.0
scipy>=1.9.0

# ─── Data Handling ──────────────────────────────────────────
pandas>=1.5.0

# ─── Signal Processing ──────────────────────────────────────
mne>=1.3.0                  # EEG signal processing and filtering
PyWavelets>=1.4.1           # Wavelet transforms for EEG analysis

# ─── Machine Learning Utilities ─────────────────────────────
scikit-learn>=1.2.0         # Metrics, train/test split, confusion matrix

# ─── Visualization ──────────────────────────────────────────
matplotlib>=3.6.0
seaborn>=0.12.0             # Heatmaps and styled plots
plotly>=5.13.0              # Interactive visualizations

# ─── Notebook Support ───────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.21.0
nbformat>=5.7.0

# ─── Progress & Utilities ───────────────────────────────────
tqdm>=4.64.0                # Training progress bars
pyyaml>=6.0                 # Config file support

# ─── Model Saving & Loading ─────────────────────────────────
h5py>=3.8.0                 # Optional: HDF5 format support

# ============================================================
#   INSTALLATION INSTRUCTIONS
# ============================================================
#
#   Standard install:
#       pip install -r requirements.txt
#
#   GPU support (CUDA 11.8):
#       pip install torch torchvision torchaudio \
#           --index-url https://download.pytorch.org/whl/cu118
#       pip install -r requirements.txt
#
#   GPU support (CUDA 12.1):
#       pip install torch torchvision torchaudio \
#           --index-url https://download.pytorch.org/whl/cu121
#       pip install -r requirements.txt
#
#   CPU only (no GPU):
#       pip install torch torchvision torchaudio \
#           --index-url https://download.pytorch.org/whl/cpu
#       pip install -r requirements.txt
#
#   Verify GPU is available after install:
#       python -c "import torch; print(torch.cuda.is_available())"
#
# ============================================================
